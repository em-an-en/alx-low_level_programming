#include "main.h"

/**
 * main - Entry point
 * @argc: number of command line args
 * @argv: array of args
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
